pipeline {
    agent any

    parameters {
        string(name: 'IBM_API_KEY', defaultValue: '', description: 'IBM Cloud API key')
        string(name: 'REPO_URL', defaultValue: 'https://github.com/your-username/terraform-repo.git', description: 'Git repository URL for Terraform code')
    }

    environment {
        IBM_API_KEY = "${params.IBM_API_KEY}"
        REPO_URL = "${params.REPO_URL}"
    }

    stages {
        stage('Checkout Code') {
            steps {
                checkout scm
            }
        }

        stage('Run Delete Cluster Script') {
            steps {
                script {
                    sh """
                    chmod +x ./manageiq-jobs/scripts/scripts/delete-cluster.sh
                    ./manageiq-jobs/scripts/scripts/delete_cluster.sh --ibm-api-key ${IBM_API_KEY} --repo-url ${REPO_URL}
                    """
                }
            }
        }
    }

    post {
        success {
            echo "Cluster deletion completed successfully!"
        }
        failure {
            echo "Cluster deletion failed. Check the logs for details."
        }
    }
}